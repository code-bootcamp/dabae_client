import styled from "@emotion/styled";

export const Wrapper = styled.div`
  width: 100%;
`;

export const AgreementTitleInner = styled.div`
  padding: 40px 0 50px;
`;

export const AgreementTitle = styled.p`
  margin: 0;
  font-size: 20px;
  font-weight: bold;
`;

export const AgreementTitleBox = styled.div`
  cursor: pointer;
  display: flex;
  align-items: center;
`;

export const Img = styled.img`
  transform: rotate(180deg);
  margin-right: 10px;
`;

export const AgreementTextBox = styled.div`
  width: 100%;
  margin-bottom: 20px;
`;

export const AgreementText = styled.p`
  color: rgb(155, 155, 155);
  line-height: 18px;
  font-size: 10px;
  word-break: break-all;
  white-space: pre-wrap;
`;
